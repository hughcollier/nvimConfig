-- Tried to get this: https://github.com/EmranMR/tree-sitter-blade/discussions/19#discussion-5400675 to 
-- work with tree-sitter but doesn't seem to work so using this for now. Because it is standalone and 
-- does not intergrate with tree-sitter then it means I do not get rainbow brackets in blade files so 
-- this is worth revisiting at some point.

return {
	 "jwalton512/vim-blade",
}
